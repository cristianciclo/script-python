#-*- coding: utf-8 -*-
  class script:
      array = []
      cadena = input('Introduzca una palabra: ')
      print (cadena, '--', len(cadena))
     print (cadena[::-1])
      #creando array de strings
       array=["casa","coche","autobus","instituto","luna"]
       for var in array:
        print (var)